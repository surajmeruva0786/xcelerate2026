services:
  - type: web
    name: xcelerate2026-backend
    env: python
    region: oregon
    buildCommand: bash render-build.sh
    startCommand: cd backend && gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
